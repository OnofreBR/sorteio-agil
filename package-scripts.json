{
  "scripts": {
    "build:client": "vite build --outDir dist/client --ssrManifest",
    "build:server": "vite build --ssr src/entry-server.tsx --outDir dist/server",
    "generate:ssg": "node scripts/generate-ssg.mjs",
    "build:complete": "npm run build:client && npm run build:server && npm run generate:ssg"
  }
}
